import React from 'react'
import PropTypes from 'prop-types'

const UnauthorizedPage: React.FC = (props) => {
  return <div>No tiene permiso para estás páginas</div>
}

UnauthorizedPage.propTypes = {}

export default UnauthorizedPage
